# Aindexo
Aindexo is a Windows-focused AI file organizer that performs full-system indexing, generates semantic embeddings for every file, and enables one-click auto-organization and search through a fast native backend and a modern UI.
